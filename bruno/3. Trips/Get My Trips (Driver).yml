info:
  name: Get My Trips (Driver)
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/trips"
  headers:
    - name: Authorization
      value: Bearer {{driver_token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
