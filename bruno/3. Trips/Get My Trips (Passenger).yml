info:
  name: Get My Trips (Passenger)
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/trips"
  headers:
    - name: Authorization
      value: Bearer {{passenger_token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
