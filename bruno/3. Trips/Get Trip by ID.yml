info:
  name: Get Trip by ID
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/trips/{{trip_id}}"
  headers:
    - name: Authorization
      value: Bearer {{passenger_token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
