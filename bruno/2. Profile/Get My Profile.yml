info:
  name: Get My Profile
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/profile"
  headers:
    - name: Authorization
      value: Bearer {{passenger_token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
